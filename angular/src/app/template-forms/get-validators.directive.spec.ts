import { GetValidatorsDirective } from './get-validators.directive';

describe('GetValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new GetValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
